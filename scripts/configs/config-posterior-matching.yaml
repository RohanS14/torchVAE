run_name: posterior_matching_experiment
model:
    name: VAE
    latent_dims: 20
    architecture: linear
    beta: 1.0
training:
    batch_size: 128
    learning_rate: 0.001
    num_epochs: 100
    save_model: true
    posterior_epochs: 30
    posterior_lr: 0.001
dataset:
    name: MNIST
    num_train: 10000
